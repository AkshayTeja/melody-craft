import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "MelodyCraft - AI-Powered Music Generation",
  description:
    "Create beautiful music with AI. Transform your ideas into professional tracks in seconds with MelodyCraft's AI-powered music generation platform.",
  keywords: [
    "AI music",
    "music generation",
    "AI composer",
    "melody generator",
    "music production",
    "AI tools for musicians",
  ],
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning className="dark">
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="dark" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
